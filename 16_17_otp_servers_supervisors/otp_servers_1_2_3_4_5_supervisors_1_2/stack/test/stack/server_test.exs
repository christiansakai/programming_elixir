defmodule ServerTest do
  use ExUnit.Case

  alias Stack.Server

  # test "pop" do
  #   Server.start_link([5, "cat", 9])

  #   assert Server.pop() == 5
  #   assert Server.pop() == "cat"
  #   assert Server.pop() == 9
  #   assert Server.pop()
  # end

  # test "push" do
  #   Server.start_link([])

  #   assert Server.push(3) == :ok
  #   assert Server.push(2) == :ok
  #   assert Server.push(1) == :ok

  #   assert Server.contents() == [1, 2, 3]

  #   assert Server.pop() == 1
  #   assert Server.pop() == 2
  #   assert Server.pop() == 3

  #   assert Server.pop()

  #   assert Server.contents() == []
  # end
end
